/**
 * TODO: initially implement :optional CSS pseudo-class
 * The `:optional` pseudo-class matches form inputs (`<input>`, `<textarea>`, `<select>`) which are not `:required`.
 *
 * See: https://caniuse.com/css-optional-pseudo
 */

export default {};
